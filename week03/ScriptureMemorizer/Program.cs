using System;

class Program
{
    static void Main(string[] args)
    {
        // Display Welcome
        // Collect input for book, chapter, verse, and end verse if a range (or just hit enter for "") and load into reference class/object
        // Collect input for verse text with a ; separating verses and load into word list.  Punctuation characters stay with the adjacent word as spaces delimit.
        // Collect input for how many words to hide with each step.
        // First display entire reference and verse.  Then with each enter (cr) randomly select words to hide, then display reference and partial verse.
        // Continue until all words are hidden, then display last step and end program
        Console.WriteLine("Hello World! This is the ScriptureMemorizer Project.");
    }
}